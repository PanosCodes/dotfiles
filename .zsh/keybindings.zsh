# Bind Up Arrow to search backward in history for commands starting with current input
bindkey '^[[A' history-search-backward

# Bind Down Arrow to search forward in history for commands starting with current input
bindkey '^[[B' history-search-forward
